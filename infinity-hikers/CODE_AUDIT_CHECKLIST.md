# Code Audit Checklist

Generated by a 5-way parallel audit (state/context layer, admin panel, hero/destination
pages, shared layout/routing, static/content pages). Each item has a concrete
`file:line` pointer and was verified by reading the actual code — nothing here is
speculative. Work through this in dependency-aware batches; check an item off only
after it's fixed **and** verified (see the audit-and-fix skill).

**Summary:** 11 Broken · 4 Wrongly wired · 2 Misplaced · 14 Could be better · 11 Should be removed (42 total)

---

## Broken

- [x] `src/context/ItineraryContext.jsx:183-185`, `TestimonialsContext.jsx:69`, `SettingsContext.jsx:52`, `SitePagesContext.jsx:124`, `PricingRulesContext.jsx:62`, `HeroContext.jsx:154` — every `reset*ToDefaults` only calls local `setState`, never writes the defaults back to Supabase; the "reset" silently reverts on the next fetch for any visitor (NavLinksContext/CatalogContext are the exception — they set `dirtyRef.current = true` so theirs actually persists). **Fixed:** all six now persist (delete+insert for row-based tables, update for the single site_config row) before updating local state. **Verified:** clean build; these functions have zero call sites anywhere in the app, so behavior change is currently latent (safe) but correct for the next caller.
- [x] `src/context/ItineraryContext.jsx:126`, `TestimonialsContext.jsx:35`, `HeroContext.jsx:93`, `SettingsContext.jsx:37`, `SitePagesContext.jsx:94-100`, `PricingRulesContext.jsx:47` — the mount-time fetch's `.then()` unconditionally overwrites state with no check for whether a mutator already changed local state while the fetch was in flight. **Fixed:** each context now sets a `mutatedRef` the moment any mutator starts, and the fetch's `.then()` bails out if it's set. **Verified:** clean build + live admin session smoke test.
- [x] `src/context/SettingsContext.jsx:43` and `PricingRulesContext.jsx:53` — stale-closure clobbering on concurrent saves. **Fixed:** both now read from a ref that's updated synchronously alongside state, not the render-closure variable. **Verified:** clean build.
- [x] `src/context/SitePagesContext.jsx:114` — `updatePage` stale-closure bug. **Fixed:** same ref pattern applied to `pages`. **Verified:** clean build.
- [x] `src/context/NavLinksContext.jsx:63` and `CatalogContext.jsx:65` — silent no-op with no error when Supabase isn't configured. **Fixed:** now sets a visible `saveError` (deferred via `setTimeout` to satisfy the set-state-in-effect lint rule, matching the file's own existing pattern). **Verified:** lint clean (no new warnings), clean build.
- [x] `src/pages/AdminPanel.jsx:221` (`openNewTour`) and `:228` (`openEditTour`) — stale upload preview bleeding between tours. **Fixed:** added `resetTourUploadState()` mirroring the hero form's equivalent, called from both. **Verified:** live admin session — new/edit tour forms open cleanly.
- [x] `src/pages/AdminPanel.jsx:630` — hero past-date validation blocked editing already-past slides. **Fixed:** the past-date check now only applies when creating a *new* slide (`!editingHeroId`), matching the tour form's behavior of not blocking edits to existing records. **Verified:** live admin session — hero edit modal opens and would allow saving.
- [x] `src/components/Preloader.jsx:98-104` — `t2` timeout cleanup was a no-op. **Fixed:** rewritten with refs (`doneTimersRef`, `doneTriggeredRef`) and a `[progress]`-only dependency. **Note:** the first attempt (keeping `[progress, phase]` as deps) caused a worse live regression — the effect's own `setPhase("done")` call re-triggered the effect, whose cleanup then cancelled `t2` before it could ever fire, hanging the preloader forever. Caught via live Playwright verification (blank page, preloader never completed) and fixed by removing `phase` from the deps and guarding with a ref instead. **Verified:** live screenshot confirms preloader completes and site renders.
- [x] `src/components/Navbar.css:35-42` — logo's `scale(4)` transform didn't match its 32px layout box. **Fixed:** measured the actual rendered size (128×128, confirmed via `getBoundingClientRect`) — reserving that much box space would balloon the whole navbar height, so instead sized the box to a normal 40×40px logo mark and removed the transform, adjusting the compensating `gap` down from `3.5rem` to `0.6rem`. **Verified:** live screenshot — logo renders cleanly, no overlap, navbar height unchanged.
- [x] `src/components/CinematicHero.jsx:244-264` — tagline rendered unconditionally, showing an empty line + cursor when blank. **Fixed:** wrapped in `{slide.tagline && (...)}`, matching the `slide.dates` guard right above it. **Verified:** clean build.
- [x] `src/pages/MapPage.jsx:62-66` — `flyTo` called in render body. **Fixed:** moved into a `useEffect` keyed on `[map, position]`. **Verified:** live test — clicked a destination card, map flew to the correct location, no console errors.

## Wrongly wired

- [x] `src/pages/AdminPanel.jsx:2175-2177` — testimonial "Destination" `<select>` hardcoded to 2 options. **Fixed:** options now derived from real tour data (`itineraries.map(i => i.country)`, deduped) plus whatever value is already stored. **Verified:** live admin session — options list now shows Sri Lanka/Indonesia/India/United Arab Emirates instead of a fixed 2-item list.
- [x] `src/pages/AdminPanel.jsx:2181-2183` — testimonial "Rating" `<select>` hardcoded to `[5,4,3]`. **Fixed:** expanded to `[5,4,3,2,1]`. **Verified:** live admin session.
- [x] `src/context/ItineraryContext.jsx:143` — `deriveScope()` missing its 3rd arg, silently defeating its own safety check. **Fixed:** removed the call (this context has no access to the admin's live `scopeOptions`, and the admin form's own `deriveScope` call already resolves the real scope before reaching here); replaced with a plain `"international"` safety-net default. Removed the now-unused `deriveScope` import. **Verified:** clean build.
- [x] `src/pages/TripPlanner.jsx:58-59` — blank `country` silently classified "international". **Fixed:** added `Boolean(country) &&` guard. **Verified:** clean build.

## Misplaced

- [x] `src/pages/AdminPanel.jsx:504` and `:613` — duplicated URL-validation regex instead of reusing `IMAGE_URL_RE`. **Fixed:** both call sites now use the shared constant. **Verified:** clean build.
- [x] `src/components/CinematicHero.jsx:64-94` — `masonryItems`'s data transform duplicated `heroSlides.js`'s pattern. **Fixed:** moved to a new `buildMasonryItems()` export in `src/utils/heroSlides.js`; `CinematicHero` now just calls it. Pure refactor, no logic changed. **Verified:** clean build, live screenshot confirms hero background renders identically.

## Could be better

- [x] `src/pages/AdminPanel.jsx:1095` — toast said "Category" instead of "Trip type". **Fixed.**
- [x] `src/pages/AdminPanel.jsx:1141` — toast said "Scope" instead of "Region". **Fixed.**
- [x] `src/pages/AdminPanel.jsx:205` — magic numbers for toast durations. **Fixed:** extracted to `NOTIFY_SUCCESS_MS`/`NOTIFY_ERROR_MS` constants.
- [x] `src/utils/analytics.js:7,13,53` — GA4 unconditionally injected despite placeholder ID. **Fixed:** added a `GA_ID_IS_PLACEHOLDER` guard matching the existing `HOTJAR_ID` pattern.
- [x] `src/utils/formatDates.js:13` — no days-in-month validation. **Fixed:** round-trips the parsed date through `new Date(y,m,d)` and rejects if it doesn't match (e.g. Feb 30 normalizes to Mar 2, which now fails the check).
- [x] `src/context/HeroContext.jsx:134-151` — `moveSlide` stale-index race. **Fixed:** the functional `setSlides` updater now re-finds both slides by `id` in `prev` instead of reusing the pre-await `i`/`j` indices.
- [x] `src/components/Navbar.jsx:22-26` and `src/components/ScrollToTop.jsx:7-11` — duplicated scroll-listener pattern. **Fixed:** extracted `src/hooks/useScrollPastThreshold.js`, used by both.
- [x] `src/components/Chatbot.jsx:94-98` — `setTimeout` not cleared on unmount. **Fixed:** stored in a ref, cleared on unmount and before scheduling a new one.
- [x] `src/components/LeadCapture.jsx:36` — same unmount-safety gap. **Fixed:** same ref-based cleanup pattern.
- [x] `src/components/Masonry.jsx:64-68` — `useMedia`'s array-literal args re-triggered its effect every render. **Fixed:** hoisted `COLUMN_QUERIES`/`COLUMN_VALUES` to module-level constants so the same reference is passed every render.
- [x] `src/components/Testimonials.jsx:29` — `key={t.name}` collision risk. **Fixed:** switched to `key={t.id}` (a real unique id already exists on every testimonial, default and admin-added).
- [x] `src/pages/DestinationsPage.jsx:194-198` — unmemoized filter feeding other `useMemo`s. **Fixed:** wrapped in `useMemo` keyed on the context's stable `itineraries` array (not the unstable `getActiveItineraries` function reference) and `category`.
- [x] `src/pages/FAQPage.jsx:20-25` — index-based key/open-state collision risk. **Fixed:** tracks the open FAQ by its question text instead of array index (FAQs have no stable id field).
- [ ] `src/pages/Community.jsx:5-61` — hardcoded mock data with no admin wiring. **Flagged, not fixed** — building real admin-configurable content for this page means a new context + CRUD UI, which is new feature work, not a bug fix (out of scope per this skill's own "don't build data/UI that doesn't exist yet" rule). Recommend a follow-up task if this page should become admin-editable like the rest of the site.

**Verified (this bucket):** clean build; lint at the same 51-problem baseline throughout; live Playwright checks — FAQ accordion opens/closes correctly and independently, Destinations page renders its filtered list with no crash, navbar/scroll-to-top behavior unchanged.

## Should be removed

- [ ] `src/pages/AdminPanel.jsx:21` — `ChevronDown`/`ChevronUp` imported from lucide-react but never used (reordering UI uses `ArrowUp`/`ArrowDown` instead).
- [ ] `src/components/CustomCursor.jsx` — never imported anywhere; dead code.
- [ ] `src/components/PageTransition.jsx` — never imported; `App.jsx`'s `AppContent` already implements equivalent transition logic inline; duplicate + unused.
- [ ] `src/components/ScrollProgress.jsx` — never imported anywhere; dead code.
- [ ] `src/components/TiltCard.jsx` — never imported anywhere; dead code.
- [ ] `src/components/TextReveal.jsx` — never imported anywhere; dead code.
- [ ] `src/components/LazyImage.jsx` — never imported anywhere; dead code.
- [ ] `src/index.css:232-235` — `@keyframes float-delay` is byte-identical to `@keyframes float` and neither is referenced by any `animation` rule; dead CSS.
- [ ] `src/components/DriftWall.jsx` + `DriftWall.css` — no longer imported anywhere after the Masonry swap; fully dead.
- [ ] `src/components/HeroSlider.jsx` + `HeroSlider.css` — never imported anywhere; superseded by `CinematicHero`, fully dead.
- [ ] `src/context/SitePagesContext.jsx:47` (`sustainability.badge` default) — read at `src/pages/Sustainability.jsx:46` but has no corresponding admin form field in `AdminPanel.jsx`'s Pages tab; permanently stuck at the hardcoded default, a dead content path from the editor's perspective.
